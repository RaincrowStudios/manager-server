{
  "name": "manager-server",
  "version": "1.0.0",
  "description": "Manager Server for Raincrow Studios",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "gcloud beta emulators datastore env-init > set_vars.cmd && set_vars.cmd && set NODE_ENV=development&& nodemon --trace-warnings server.js"
  },
  "author": "Sean Landis",
  "license": "SEE LICENSE IN LICENSE",
  "repository": "github:RaincrowStudios/manager-server",
  "dependencies": {
    "@google-cloud/datastore": "^1.3.4",
    "lokijs": "^1.5.3",
    "redis": "^2.8.0",
    "uuid": "^3.2.1"
  },
  "devDependencies": {
    "eslint": "^4.18.1",
    "eslint-plugin-node": "^6.0.1",
    "nodemon": "^1.15.1"
  }
}
